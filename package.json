{
  "name": "amphi-calls",
  "private": true,
  "scripts": {
    "dev:web": "pnpm --filter web-app dev",
    "dev:signal": "pnpm --filter signaling-server dev",
    "build": "pnpm -r build",
    "check": "pnpm -r check"
  }
}
